  <% if current_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(current_object.errors.count, "error") %> prohibited this property from being saved:</h2>

      <ul>
      <% current_object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>